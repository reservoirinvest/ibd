"""NiceGUI dashboard (quant-ops terminal) — Track 2 migration target.

Runs alongside the Streamlit `app.py` until parity (see the migration plan).
Entry point: `web/main.py` → `uv run python web/main.py`.
"""
